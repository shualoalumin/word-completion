# 🏥 Project Health Check Report

> **Date**: 2026-01-16  
> **Scope**: FE/BE 전체 아키텍처, 코드 품질, 확장성 점검  
> **Status**: 🟡 개선 필요

---

## 📊 Executive Summary

| 영역 | 상태 | 점수 | 비고 |
|------|------|------|------|
| **폴더 구조** | 🟢 양호 | 8/10 | Feature-based 구조 잘 적용됨 |
| **타입 안전성** | 🔴 심각 | 4/10 | Supabase 타입 미동기화 |
| **ESLint 설정** | 🔴 심각 | 3/10 | `no-undef` 미설정으로 런타임 에러 발생 |
| **테스트** | 🔴 없음 | 0/10 | 테스트 파일 전무 |
| **문서화** | 🟢 양호 | 8/10 | 상세 문서 존재, 일부 업데이트 필요 |
| **코드 품질** | 🟡 보통 | 6/10 | 일관성 있으나 개선 여지 있음 |
| **확장성** | 🟢 양호 | 8/10 | 스키마 설계 잘 됨, 구현 진행 중 |

**전체 점수**: **52/70 (74%)**

---

## 🔴 Critical Issues (즉시 수정 필요)

### Issue 1: Supabase 타입 동기화 미흡

**현재 상태**:
```
src/integrations/supabase/types.ts에 정의된 테이블:
- ✅ exercises
- ✅ user_exercise_history
- ✅ user_profiles

DB에 존재하지만 타입 미정의:
- ❌ user_vocabulary
- ❌ user_vocabulary_reviews
- ❌ user_vocabulary_metrics
- ❌ user_vocabulary_growth
- ❌ user_streaks
```

**영향**:
- TypeScript 타입 체크 불가
- API 호출 시 `any` 타입 사용 → 런타임 에러 가능성
- IDE 자동완성 미작동

**해결책**:
```bash
# Supabase CLI로 타입 자동 생성
npx supabase gen types typescript --project-id <PROJECT_ID> > src/integrations/supabase/types.ts
```

---

### Issue 2: ESLint `no-undef` 규칙 미설정

**현재 상태** (`eslint.config.js`):
```javascript
rules: {
  ...reactHooks.configs.recommended.rules,
  "react-refresh/only-export-components": ["warn", { allowConstantExport: true }],
  "@typescript-eslint/no-unused-vars": "off",  // ← 이것도 문제
}
// no-undef 규칙 없음!
```

**영향**:
- `cn is not defined` 같은 런타임 에러가 빌드 시 잡히지 않음
- 4일간 에러 잠복 (01-12 → 01-16)

**해결책**:
```javascript
rules: {
  ...reactHooks.configs.recommended.rules,
  "react-refresh/only-export-components": ["warn", { allowConstantExport: true }],
  "@typescript-eslint/no-unused-vars": "warn",  // off → warn
  "no-undef": "error",  // 추가
}
```

---

### Issue 3: 테스트 완전 부재

**현재 상태**:
- 단위 테스트: 0개
- 통합 테스트: 0개
- E2E 테스트: 0개

**영향**:
- 회귀 버그 감지 불가
- 리팩토링 시 안전망 없음
- 배포 전 검증 자동화 불가

**해결책** (우선순위 순):
1. 주요 페이지 렌더링 테스트 (Smoke Test)
2. API 함수 단위 테스트
3. 핵심 플로우 E2E 테스트

---

## 🟡 Medium Issues (개선 권장)

### Issue 4: Feature 모듈 구조 불일관

**현재 구조**:
```
features/
├── auth/
│   ├── components/  ✅
│   ├── hooks/       ✅
│   └── index.ts     ✅
├── dashboard/
│   ├── api.ts       ✅
│   ├── hooks/       ✅
│   └── index.ts     ✅
├── reading/
│   └── text-completion/
│       ├── api.ts       ✅
│       ├── components/  ✅
│       ├── hooks/       ✅
│       ├── types.ts     ✅
│       └── index.tsx    ✅
└── vocabulary/
    ├── api.ts       ✅
    ├── hooks/       ⚠️ 폴더 아님 (useVocabulary.ts 직접)
    ├── review/
    │   └── api.ts   ⚠️ hooks 없음
    └── index.ts     ✅
```

**개선 방향**:
```
features/vocabulary/
├── api.ts
├── components/        ← 추가 (공통 컴포넌트)
├── hooks/
│   ├── index.ts
│   └── useVocabulary.ts
├── review/
│   ├── api.ts
│   ├── components/    ← 추가
│   └── hooks/         ← 추가
├── types.ts           ← 추가 (VocabularyWord 등)
└── index.ts
```

---

### Issue 5: API 응답 타입 불일관

**문제 패턴**:
```typescript
// vocabulary/api.ts - 일관된 패턴 ✅
async function getVocabularyList(): Promise<{
  data: VocabularyWord[] | null;
  error: Error | null;
}>

// dashboard/api.ts - 다른 패턴 ⚠️
async function getExercisesToday(userId: string): Promise<number>
async function getDashboardStats(userId: string): Promise<DashboardStats>
```

**개선 방향**: 통일된 응답 타입 사용
```typescript
// core/types/api.ts
interface ApiResponse<T> {
  data: T | null;
  error: Error | null;
}

// 모든 API 함수에 적용
async function getDashboardStats(userId: string): Promise<ApiResponse<DashboardStats>>
```

---

### Issue 6: project-status.md 업데이트 필요

**현재 상태**:
- 마지막 업데이트: 2026-01-11 (오후)
- 현재: 2026-01-16

**누락된 내용**:
- 01-15: Demo Mode 수정
- 01-16: `cn` import 에러 수정
- COOP 정책 에러 처리

---

## 🟢 Good Practices (잘 된 점)

### 1. Feature-based 폴더 구조
```
✅ 기능별로 잘 분리됨
✅ 각 feature가 독립적
✅ core/ 공통 모듈 분리
```

### 2. 상세한 문서화
```
✅ docs/architecture/ - 전략 문서
✅ docs/dev-logs/ - 개발 기록
✅ docs/troubleshooting/ - 문제 해결 기록
✅ docs/migrations/ - DB 마이그레이션
```

### 3. Clean In, Clean Out 원칙
```
✅ Edge Function에서 데이터 정규화
✅ Self-Healing 로직 (spacing)
✅ 캐싱 전략 (90% cache, 10% fresh)
```

### 4. Global First 스키마 설계
```
✅ 46개 테이블 전체 스키마 설계 완료
✅ 확장성 고려한 JSONB 필드 사용
✅ RLS 정책 일관되게 적용
```

---

## 📋 Action Items (우선순위)

### P0: 즉시 (오늘)
- [x] Dashboard `cn` import 에러 수정 ✅
- [ ] ESLint `no-undef` 규칙 추가
- [ ] `npm run lint` 실행 후 에러 수정

### P1: 이번 주
- [ ] Supabase 타입 재생성 (supabase gen types)
- [ ] vocabulary feature 구조 정리
- [ ] project-status.md 업데이트

### P2: 다음 주
- [ ] 주요 페이지 렌더링 테스트 추가
- [ ] API 응답 타입 통일
- [ ] Pre-commit hook (Husky) 설정

### P3: 이번 달
- [ ] E2E 테스트 (Playwright/Cypress) 도입
- [ ] 에러 모니터링 (Sentry) 도입
- [ ] CI/CD 파이프라인 강화

---

## 📊 FE/BE 간 데이터 흐름 점검

### 현재 플로우 (정상 작동 확인 필요)

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
├─────────────────────────────────────────────────────────────────┤
│  Pages                 Features                    Core          │
│  ┌─────────┐          ┌────────────────────┐     ┌──────────┐  │
│  │ Landing │          │ auth/              │     │ types/   │  │
│  │ Dashboard│─────────│ ├─ useAuth.ts     │     │ hooks/   │  │
│  │ Practice│          │ └─ AuthModal.tsx  │     │ utils/   │  │
│  │ Vocabulary│        │                    │     │ constants│  │
│  └─────────┘          │ dashboard/         │     └──────────┘  │
│       │               │ ├─ api.ts         │          │         │
│       │               │ └─ useDashboardStats│        │         │
│       │               │                    │          │         │
│       │               │ vocabulary/        │          │         │
│       │               │ ├─ api.ts         │          │         │
│       │               │ └─ useVocabulary  │          │         │
│       │               │                    │          │         │
│       │               │ reading/text-completion/     │         │
│       │               │ ├─ api.ts         │          │         │
│       │               │ ├─ useTextCompletion│        │         │
│       │               │ └─ components/    │          │         │
│       │               └────────────────────┘          │         │
│       │                        │                      │         │
│       ▼                        ▼                      ▼         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              integrations/supabase/client.ts              │  │
│  │              ⚠️ types.ts 미동기화 (5개 테이블 누락)         │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS (Auth Token)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                          BACKEND                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┐    ┌──────────────────────────────┐   │
│  │  Supabase Auth      │    │  Edge Functions              │   │
│  │  - Google OAuth     │    │  ├─ generate-passage/        │   │
│  │  - Session Mgmt     │    │  │   ├─ Optional Auth ✅     │   │
│  └─────────────────────┘    │  │   ├─ Cache Strategy ✅    │   │
│                             │  │   └─ AI Generation ✅     │   │
│                             │  │                           │   │
│                             │  └─ _shared/                 │   │
│                             │      ├─ ai/client.ts         │   │
│                             │      ├─ ai/gemini.ts         │   │
│                             │      └─ normalize-spacing.ts │   │
│                             └──────────────────────────────┘   │
│                                            │                    │
│                                            ▼                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Supabase Database                      │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │ 구현됨 (8/46 = 17.4%)                                │ │  │
│  │  │ ✅ exercises (55개 캐시)                             │ │  │
│  │  │ ✅ user_profiles                                     │ │  │
│  │  │ ✅ user_exercise_history                             │ │  │
│  │  │ ✅ user_streaks (PostgreSQL Trigger)                 │ │  │
│  │  │ ✅ user_vocabulary                                   │ │  │
│  │  │ ✅ user_vocabulary_reviews                           │ │  │
│  │  │ ✅ user_vocabulary_metrics                           │ │  │
│  │  │ ✅ user_vocabulary_growth                            │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │ 미구현 (38/46)                                       │ │  │
│  │  │ ⏳ user_skills, user_usage_limits, user_review_queue │ │  │
│  │  │ ⏳ achievements, user_achievements, leaderboard      │ │  │
│  │  │ ⏳ user_follows, study_groups, challenges            │ │  │
│  │  │ ⏳ subscriptions, payment_history, promo_codes       │ │  │
│  │  │ ⏳ analytics_events, daily_metrics, experiments      │ │  │
│  │  │ ⏳ audit_logs, data_deletion_requests                │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔧 개선된 ESLint 설정 제안

```javascript
// eslint.config.js
import js from "@eslint/js";
import globals from "globals";
import reactHooks from "eslint-plugin-react-hooks";
import reactRefresh from "eslint-plugin-react-refresh";
import tseslint from "typescript-eslint";

export default tseslint.config(
  { ignores: ["dist"] },
  {
    extends: [js.configs.recommended, ...tseslint.configs.recommended],
    files: ["**/*.{ts,tsx}"],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
    plugins: {
      "react-hooks": reactHooks,
      "react-refresh": reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      "react-refresh/only-export-components": ["warn", { allowConstantExport: true }],
      
      // 핵심 규칙 (에러 방지)
      "no-undef": "error",  // ✅ undefined 변수 사용 금지
      "@typescript-eslint/no-unused-vars": ["warn", { 
        "argsIgnorePattern": "^_",
        "varsIgnorePattern": "^_" 
      }],
      
      // 코드 품질
      "no-console": ["warn", { "allow": ["warn", "error"] }],
      "prefer-const": "warn",
      
      // Import 정렬 (선택사항)
      // "import/order": ["warn", { "newlines-between": "always" }]
    },
  },
);
```

---

## 📝 다음 단계 권장 사항

### 단기 (1주)
1. **즉시 수정**: ESLint 강화, Supabase 타입 동기화
2. **Pre-commit hook**: Husky + lint-staged 설정
3. **project-status.md** 업데이트

### 중기 (1개월)
1. **테스트 도입**: Vitest + React Testing Library
2. **E2E 테스트**: Playwright (주요 플로우)
3. **에러 모니터링**: Sentry 연동

### 장기 (3개월)
1. **CI/CD 강화**: GitHub Actions + 자동 배포
2. **성능 모니터링**: Web Vitals, Lighthouse CI
3. **코드 스플리팅**: 페이지별 Lazy Loading

---

## 📚 관련 문서

- `docs/project-status.md` - 프로젝트 현황
- `docs/architecture/database-schema.md` - DB 스키마
- `docs/troubleshooting/2026-01-16-cn-import-missing-root-cause-analysis.md` - 에러 분석
- `AGENTS.md` - AI 코드 생성 규칙 (업데이트 필요)

---

**작성자**: AI Assistant  
**검토 필요**: 프로젝트 오너  
**다음 점검**: 2026-01-23 (1주 후)
